<header>
<div class="navbar">
  <div class="navbar-inner">
    <div class="container">

      <%= link_to 'ToDo List', root_path, id: 'logo' %>

      <ul class="nav pull-right">
	<% if user_signed_in? %>
	  <p>Signed in as <%= current_user.username %>.</p>
	  <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>
	<% else %>
	  <li><p>Please <%= link_to "Sign up", new_user_registration_path %> or <%= link_to "Sign in", new_user_session_path %></li></p>
	<% end %>
      </ul>
    </div>
  </div>
</div>
</header>
